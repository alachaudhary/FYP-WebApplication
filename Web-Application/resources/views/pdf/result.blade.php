<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Deepfake Analysis Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
            color: #333;
        }

        .header {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 30px;
        }

        .header img {
            width: 60px;
            height: 60px;
            object-fit: contain;
        }

        .header .brand {
            font-size: 26px;
            font-weight: bold;
            color: #0e7490;
            margin-left: 0px;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #2c3e50;
        }

        .user-info {
            margin-bottom: 20px;
            font-size: 14px;
        }

        .results table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .results th, .results td {
            border: 1px solid #ccc;
            padding: 10px;
        }

        .notes {
            margin-top: 30px;
            background: #fdf6e3;
            padding: 10px;
            font-size: 14px;
            border-left: 4px solid #f39c12;
        }

        footer {
            margin-top: 50px;
            text-align: center;
            font-size: 12px;
            color: #999;
        }
    </style>
</head>
<body>

    <div class="header">
        <img src="{{ public_path('storage/logo.png') }}" alt="Logo">
        <div class="brand">FauxShield</div>
    </div>

    <h1>Deepfake Analysis Report</h1>

    <div class="user-info">
        <strong>Name:</strong> {{ $user->name }}<br>
        <strong>Email:</strong> {{ $user->email }}<br>
        <strong>Phone:</strong> {{ $user->phone ?? 'N/A' }}<br>
        <strong>Location:</strong> {{ $user->location ?? 'N/A' }}<br>
        <strong>Bio:</strong> {{ $user->bio ?? 'N/A' }}
    </div>

    <div class="results">
        <table>
            <tr>
                <th>Filename</th>
                <td>{{ $filename }}</td>
            </tr>
            <tr>
                <th>Prediction</th>
                <td>{{ $prediction }}</td>
            </tr>
            <tr>
                <th>Confidence</th>
                <td>{{ $confidence }}%</td>
            </tr>
            <tr>
                <th>Frames</th>
                <td>{{ $frames }}</td>
            </tr>
            <tr>
                <th>Processing Time</th>
                <td>{{ $processing_time }} seconds</td>
            </tr>
        </table>
    </div>

    <div class="notes">
        ⚠️ <strong>Precautionary Notes</strong><br>
        • This report is generated by an AI system.<br>
        • Always verify findings before public or legal use.<br>
        • FauxShield is not liable for misuse or misinterpretation.
    </div>

    <footer>
        Generated on {{ \Carbon\Carbon::now()->format('F j, Y, g:i a') }}<br>
        &copy; {{ date('Y') }} <strong>FauxShield</strong>. All rights reserved.
    </footer>

</body>
</html>
